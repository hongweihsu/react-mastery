import {render,screen} from "@testing-library/react";import userEvent from "@testing-library/user-event";import {expect,it} from "vitest";import Lab from "./Lab";it("saves the name through visible behaviour",async()=>{const user=userEvent.setup();render(<Lab/>);await user.type(screen.getByRole("textbox",{name:/Name/}),"Lin");await user.click(screen.getByRole("button",{name:/Save profile/}));expect(screen.getByRole("status")).toHaveTextContent("Saved Lin")});
