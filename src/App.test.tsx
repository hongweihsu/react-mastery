import { render, screen } from "@testing-library/react";import { describe,it,expect } from "vitest";import App from "./App";describe("lab runner",()=>{it("exposes the first lab through accessible controls",async()=>{render(<App/>);expect(screen.getByRole("heading",{name:/React Mastery Browser Labs/})).toBeInTheDocument();expect(screen.getByRole("combobox",{name:/Lab/})).toHaveValue("01-component-execution");expect(await screen.findByRole("heading",{name:/Execution/})).toBeInTheDocument()})});
